# Full-Stack Example

A complete application using pico-boot with configuration, services, and providers.

## Requirements

- Python 3.11+

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
python -m myapp.main
```

This will:
1. Load configuration from `application.yaml`
2. Initialize all components with dependency injection
3. Demonstrate service interactions
